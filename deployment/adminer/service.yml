services:
  # Database Web UI
  adminer:
    image: adminer:latest
    container_name: adminer
    ports:
      - "${ADMINER_PORT}:8080"
    environment:
      ADMINER_DEFAULT_SERVER: ${ADMINER_DEFAULT_SERVER}
      TZ: Asia/Jakarta
    restart: always
    networks:
      - fyrn
